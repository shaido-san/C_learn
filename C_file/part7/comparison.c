# include <stdio.h>

int main(void)
{
    int suuti = 10;
    int suuti2;
    if (suuti) printf("%d\n", suuti);

    scanf("%d",&suuti);
    if (suuti2 - 10) printf("入力値は10ではありません。\n");
    return 0;
}